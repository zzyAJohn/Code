#include "stdio.h"
#include "math.h"
int hannuota(int x,int y)
{
    int m;
    m=sqrt(x+y);
    m*m==(x+y)?yse:no;
    return 0;
}
int main()
{
    int n,a[100],i;
    printf("输入：");
    scanf("%d",&n);
    a[0]=n;
    for(i=0;i<=n;i++);              //定义一个数组存储输入的数据
    {
        scanf("%d",a[i]);
    }
    hannuota(1,3);
}