#include "stdio.h"
int main()
{
    int a,b,k;
    for(a=1;a<2;b+1)
     {
       switch(k)
         {
           case 1:a++;break;
           default:printf("你输错了哦小宝贝~");
         }
           if(k!=1)                    //如果不是所要值，则再输入一次
               scanf("%d",&k);
     }
}